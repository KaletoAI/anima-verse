// Thin path alias: the magnifier + the enlarge-props factory live in the
// shared package so the player panels and the Game-Admin use the same one.
export { ZoomButton, useEnlarge } from '@anima/player-ui'
