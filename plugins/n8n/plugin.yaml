name: n8n
version: "1.2.0"
description: Calls n8n workflows via webhook (POST) and returns the JSON response to the LLM
skills:
  - skill_id: n8n
    always_load: true
templates:
  llm: templates/llm
